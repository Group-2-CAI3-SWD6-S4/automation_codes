package RemoveProductsFromCart_pkg;

import Pages_pkg.CartPage;
import base_pkg.base_class;
import org.testng.Assert;
import org.testng.annotations.Test;

public class RemoveProductsFromCart extends base_class {
    @Test
    public void RemoveProductsFromCart_tc(){
        String productOneId;
        String productTwoId;

        homePage.AddProductOne();

        productOneId = homePage.GetProductOneId();

        CartPage cartPage = homePage.clickViewCartFromModal();
            Assert.assertTrue(cartPage.ChechShoppingCartTextIsVisible());

        cartPage.DeleteById(productOneId);

        Assert.assertFalse(cartPage.CartIsEmpty());

    }
}
