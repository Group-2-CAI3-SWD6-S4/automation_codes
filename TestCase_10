package TestCase_10_pkg;

import base_pkg.base_class;
import org.testng.Assert;
import org.testng.annotations.Test;

public class TestCase_10 extends base_class {
    @Test
    public void VerifySubscriptioninhomepage_tc(){
        boolean isHomeVisible = homePage.verifyHomePage();
        Assert.assertTrue(isHomeVisible, "Home page is not visible!");
        homePage.scrollDownUntilSubscriptionVisible();
        homePage.InsertEmail("mohamed@gmail.com");
        homePage.ClickSubmitButton();

        Assert.assertTrue(homePage.isSuccessfullySubscribedVisible());

    }
}
